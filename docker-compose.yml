version: '3'

services:
  app:
    image: fffaraz/php:latest
    restart: always
    volumes:
      - ./app:/app
      - ./socket:/socket
      - mariadb1:/db

volumes:
  mariadb1:
    external: true
